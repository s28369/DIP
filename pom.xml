<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.1</version>
        <relativePath/>
    </parent>

    <groupId>org.example</groupId>
    <artifactId>fleet-management</artifactId>
    <version>1.0-SNAPSHOT</version>
    <name>Fleet Management System</name>
    <description>Uproszczona aplikacja do zarządzania flotą transportową</description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <javafx.version>21.0.1</javafx.version>
        <javafx.m2.basedir>${user.home}/.m2/repository/org/openjfx</javafx.m2.basedir>
        <!-- Классификатор по умолчанию (переопределяется профилем) -->
        <javafx.classifier>mac-aarch64</javafx.classifier>
    </properties>

    <dependencies>
        <!-- Spring Boot Starter Data JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- H2 Database (do testów) -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- PostgreSQL Driver (opcjonalnie) -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- JavaFX (bez klasyfikatora = wersja niezależna od platformy) -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <!-- Spring Boot Starter Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>javafx-win</id>
            <activation><os><family>windows</family></os></activation>
            <properties><javafx.classifier>win</javafx.classifier></properties>
        </profile>
        <profile>
            <id>javafx-linux</id>
            <activation><os><family>unix</family><arch>x86_64</arch></os></activation>
            <properties><javafx.classifier>linux</javafx.classifier></properties>
        </profile>
        <profile>
            <id>javafx-mac-x64</id>
            <activation><os><family>mac</family><arch>x86_64</arch></os></activation>
            <properties><javafx.classifier>mac</javafx.classifier></properties>
        </profile>
        <profile>
            <id>mac-aarch64</id>
            <activation>
                <os><arch>aarch64</arch><family>mac</family></os>
            </activation>
            <properties><javafx.classifier>mac-aarch64</javafx.classifier></properties>
            <dependencies>
                <dependency>
                    <groupId>org.openjfx</groupId>
                    <artifactId>javafx-base</artifactId>
                    <version>${javafx.version}</version>
                    <classifier>mac-aarch64</classifier>
                </dependency>
                <dependency>
                    <groupId>org.openjfx</groupId>
                    <artifactId>javafx-graphics</artifactId>
                    <version>${javafx.version}</version>
                    <classifier>mac-aarch64</classifier>
                </dependency>
                <dependency>
                    <groupId>org.openjfx</groupId>
                    <artifactId>javafx-controls</artifactId>
                    <version>${javafx.version}</version>
                    <classifier>mac-aarch64</classifier>
                </dependency>
                <dependency>
                    <groupId>org.openjfx</groupId>
                    <artifactId>javafx-fxml</artifactId>
                    <version>${javafx.version}</version>
                    <classifier>mac-aarch64</classifier>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <includes>
                    <include>**/*.*</include>
                </includes>
            </resource>
        </resources>
        
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>org.example.fleetmanagement.FleetManagementApplication</mainClass>
                </configuration>
            </plugin>

            <!-- Hibernate Bytecode Enhancement — lazy loading for @Basic fields -->
            <plugin>
                <groupId>org.hibernate.orm.tooling</groupId>
                <artifactId>hibernate-enhance-maven-plugin</artifactId>
                <version>6.6.5.Final</version>
                <executions>
                    <execution>
                        <id>enhance</id>
                        <goals><goal>enhance</goal></goals>
                        <configuration>
                            <enableLazyInitialization>true</enableLazyInitialization>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- JavaFX Maven Plugin: использует JAR из Maven-репозитория -->
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>org.example.fleetmanagement.FleetManagementApplication</mainClass>
                    <options>
                        <option>--module-path</option>
                        <option>${javafx.m2.basedir}/javafx-base/${javafx.version}/javafx-base-${javafx.version}-${javafx.classifier}.jar:${javafx.m2.basedir}/javafx-graphics/${javafx.version}/javafx-graphics-${javafx.version}-${javafx.classifier}.jar:${javafx.m2.basedir}/javafx-controls/${javafx.version}/javafx-controls-${javafx.version}-${javafx.classifier}.jar:${javafx.m2.basedir}/javafx-fxml/${javafx.version}/javafx-fxml-${javafx.version}-${javafx.classifier}.jar</option>
                        <option>--add-modules</option>
                        <option>javafx.controls,javafx.fxml</option>
                    </options>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>